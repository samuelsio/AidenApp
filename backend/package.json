{
    "name": "backend",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "start": "node src/index.js",
        "test": "echo \"Error: no test specified\" && exit 1",
        "db-start": "docker-compose up --build",
        "db-init": "docker-compose exec db psql -U app_user -d testdb -f /docker-entrypoint-initdb.d/init.sql",
        "db-save": "docker-compose exec db pg_dump -U app_user testdb > ./backend/db/init.sql"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "description": "",
    "dependencies": {
        "bcrypt": "^5.1.1",
        "body-parser": "^1.20.2",
        "dotenv": "^16.4.5",
        "express": "^4.19.2",
        "pg": "^8.12.0"
    }
}
